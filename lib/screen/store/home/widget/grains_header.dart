import 'package:flutter/material.dart';

import 'header.dart';

class GrainsHeader extends Header {
  const GrainsHeader({Key? key, VoidCallback? onPressed})
      : super(
          key: key,
          onPressed: onPressed,
          heading: 'Grains',
        );
}
